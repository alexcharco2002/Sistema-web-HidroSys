# .env (archivo en la raiz del proyecto)
# contiene variables de entorno para la configuracion de la aplicacion

# Database
DATABASE_URL=postgresql://postgres:TecniCobro2024@localhost:5432/jaap_sanjapamba
DB_NAME=jaap_sanjapamba
DB_USER=postgres
DB_PASSWORD=TecniCobro2024
DB_HOST=localhost
DB_PORT=5432

# JWT Security
SECRET_KEY=tu_clave_secreta_muy_segura_aqui_minimo_32_caracteres
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60  

# SSL/HTTPS Configuration
SSL_ENABLED=true
SSL_CERT_FILE=certs/cert.pem
SSL_KEY_FILE=certs/key.pem

# API Configuration
API_HOST=0.0.0.0
API_PORT=8001
API_PROTOCOL=https

# CORS Origins (separados por coma)
ALLOWED_ORIGINS=https://localhost:3000,https://127.0.0.1:3000,http://localhost:3000

# Environment
ENVIRONMENT=development

# SMTP (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=sanjapambaj@gmail.com
SMTP_PASSWORD=erjfpwoufjhvosls
SMTP_FROM=sanjapambaj@gmail.com
SMTP_FROM_NAME=JAAP Sanjapamba

# Codigos de verificacion
VERIFICATION_CODE_EXPIRE_MINUTES=15

# Backup settings
BACKUP_DIR=backups
